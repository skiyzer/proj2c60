import './App.js';
import tablerow from './tablerow.js';

function table()
{
    <tr>
        <thead className={result.grant.Title}>
            {result.Grant.title}
            {result.Grant.institution}
        </thead>
    </tr>
}